apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: test-secret
stringData:
  name: Vivek Hiwarkar
  rank: Diamond
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: test-deployment
    # Mandatory field
    app: secret-deployer
    # Mandatory field
    secret-name: test-secret
  name: test-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: test-deployment
  template:
    metadata:
      labels:
        app: test-deployment
    spec:
      containers:
      - command:
        - ping
        - 8.8.8.8
        image: busybox:latest
        name: busybox